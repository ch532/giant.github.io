<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="your.package.name">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Optional: For better targeting -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:hardwareAccelerated="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Start.io SDK metadata -->
        <meta-data
            android:name="com.startapp.sdk.APPLICATION_ID"
            android:value="205787982" />
        <meta-data
            android:name="com.startapp.sdk.DEV_ID"
            android:value="176855992" />

        <!-- Optional: Allow return ads -->
        <meta-data
            android:name="com.startapp.sdk.RETURN_AD_ENABLED"
            android:value="false" />

        <!-- Unity Ads SDK metadata -->
        <meta-data
            android:name="unity.ads.gameId"
            android:value="5883117" />
        <meta-data
            android:name="unity.ads.testMode"
            android:value="false" />
    </application>

</manifest>
